import React from "react";
import {Page} from "../components/Page";

export const PageNotFound = () => {
    return <Page titulo="Página no encontrada" descripcion={"La página solicitada no existe"}/>;
}
